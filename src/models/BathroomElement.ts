export default class BathroomElement {
    type: string;
    width: number;
    x: number;

    constructor(t: string, w: number, x: number) {
        this.type = t;
        this.width = w;
        this.x = x;
    }

}